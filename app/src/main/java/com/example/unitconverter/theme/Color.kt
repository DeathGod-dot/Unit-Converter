package com.example.unitconverter.theme

import androidx.compose.ui.graphics.Color

// ─── App background & card surfaces ──────────────────────────────────────────
val AppBackground   = Color(0xFF0B0D12)  // near-black, slightly blue
val CardBackground  = Color(0xFF141721)  // card surface
val CardBorder      = Color(0xFF1F232E)  // card border, 1dp
val MutedText       = Color(0xFF6B7284)  // muted / secondary text
val PrimaryText     = Color.White        // primary text

// ─── Inactive chip colors ─────────────────────────────────────────────────────
val ChipInactiveBorder = Color(0xFF242938)
val ChipInactiveFill   = Color(0xFF151822)
val ChipInactiveText   = Color(0xFF8B91A1)

// ─── Controls row ─────────────────────────────────────────────────────────────
val ControlsReadOnly = Color(0xFF8B91A1)  // read-only "To" result color
